optimization: add goal-aware model routing to lib/route.mjs — currently opusMinRisk:3 sends ~80% of tasks to strong tier (Opus). Change: (1) raise opusMinRisk default to 4 in the routing rules; (2) add a goalTierOverride table in config: goal∈{intelligence, optimization, ui, reliability, maintenance} + risk<=3 → mid; goal∈{architecture, migration} + risk>=3 → strong (unchanged). This means standard intelligence/optimization tasks at risk 3 use Sonnet (~10x cheaper) while genuinely complex architecture work still gets Opus. Backward compatible: existing config with explicit opusMinRisk:3 overrides the new default. goal: optimization risk: 2
