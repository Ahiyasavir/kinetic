optimization: decouple adaptive rate-limit recovery from business cooldown in supervisor.mjs — currently adaptiveIntervalMs reaches 43+ minutes even when velocityFactor < 0.5 (well under budget) and no 429 has occurred. The fix: only increaseAdaptiveInterval() when an actual RateLimitError is thrown (keep current AIMD logic there); add a separate minCooldownMs (default 90000 = 90s) that applies between cycles when no rate limit is active and velocityFactor < 0.8. When velocityFactor < 0.5 AND no 429 in last N cycles, use minCooldownMs directly. This reduces backlog completion time from ~38 hours to ~2-3 hours on a dedicated account. goal: optimization risk: 3
