intelligence: call recordLesson on re-queued-smaller and re-queued-with-memory outcomes in supervisor.mjs — currently recordLesson is called only on blocked/rollback/high-revision/crash (3 call sites). Tasks that are re-queued (8.8% of cycles) fail silently without contributing to lessons.json. Fix: in the re-queued branch of runCycle, call recordLesson with failureType:'re-queued' and the available impl/validation/review context. This captures patterns like "task was too large for one cycle" or "implementer consistently misunderstood the scope" — patterns that currently repeat indefinitely. After this change, re-queued tasks should stop repeating the same mistake on the next attempt. goal: intelligence risk: 2
