intelligence: implement blocked-queue auto-review — tasks in state.queues.blocked sit forever with no recovery path. Every 10 cycles, if blocked.length > 0, invoke a short Haiku call with: (1) the blocked task's blockReason, (2) a summary of what changed in the codebase since it was blocked (git log --oneline since task.doneCycle or task.createdCycle). Haiku returns {unblock: bool, reason: string}. If unblock===true: move task back to backlog, clear blockReason, reset noChangeAttempts, log the recovery. If unblock===false: update task.blockReviewedAt and skip for another 10 cycles. This recovers tasks blocked by now-fixed root causes (e.g. tasks blocked before task-class.mjs was added). goal: intelligence risk: 3
