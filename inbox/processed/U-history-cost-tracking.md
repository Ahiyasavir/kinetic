optimization: add per-cycle cost and duration tracking to history entries in supervisor.mjs — current history entries contain only {cycle, taskId, title, outcome, ts}. Add: costUsd (from currentCycleTokens() at end of cycle), inputTokens, outputTokens, cacheReadTokens, durationMs (Date.now() diff from cycle start), modelTier (from route result). This enables: (1) identifying expensive vs cheap task types, (2) per-goal cost breakdowns for the /api/stats endpoint, (3) actual ROI data for product marketing ("architecture tasks average $1.20, UI tasks average $0.60"). Change is additive — only the history push site in supervisor.mjs needs updating. goal: optimization risk: 1
