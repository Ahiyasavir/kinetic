intelligence: implement an auto-documentation sync step — after a successful merge (post-validate, pre-cooldown), diff the list of changed files against CLAUDE.md, TECH_SPEC.md, and STRUCTURE.md. If any changed file is architecture-relevant (new module in lib/|core/|src/, new callable in functions/src/index.ts, new page in apps/admin/src/pages/ or apps/mobile/app/), call a lightweight Haiku agent with the diff + current doc section and ask it to generate a minimal patch (<20 lines) to keep the doc current. The patch is committed to the same branch as an addendum commit "docs: sync architecture notes". Only runs when the changed files match the heuristic — skips cleanups and minor UI tweaks. Prevents CLAUDE.md drift which degrades Selector quality over time. goal: intelligence risk: 3
