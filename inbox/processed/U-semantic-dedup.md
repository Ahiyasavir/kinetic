optimization: upgrade task deduplication from Jaccard token overlap to semantic similarity — the current titlesNearDuplicate uses word-level Jaccard (threshold 0.6), which misses pairs like "fix auth validation" vs "resolve authentication check failure". Replace with a two-stage check: (1) keep Jaccard as a fast pre-filter, (2) for pairs scoring 0.3-0.6 Jaccard (ambiguous zone), run a single Haiku call with both titles asking "are these the same task? answer yes/no" — costs ~$0.0001 per check. Cache results in state/dedup-cache.json keyed by sorted title pair hash to avoid re-checking the same pair each cycle. This reduces wasted cycles on near-duplicate tasks that currently slip through and get implemented twice. goal: optimization risk: 2
