intelligence: add cycle cost analytics — track per-task actual cost (inputTokens + outputTokens + cacheRead) in app.stats keyed by [goal][risk], compute rolling averages per category, and expose GET /api/stats/cost in ui/server.mjs returning { byGoal, byRisk, totalCycles, avgCostPerCycle, projectedWeeklyCost }. The Tauri dashboard (A-S3B) can then render a "cost per task type" breakdown so the operator knows where budget goes. Store the per-task cost snapshot in each history entry at merge time so it survives state resets. goal: intelligence risk: 2
