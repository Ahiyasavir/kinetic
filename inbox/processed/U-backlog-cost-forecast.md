intelligence: implement a backlog cost forecaster — at the start of each cycle, after the Selector ranks tasks but before implementation begins, compute an estimate of total remaining cost to clear the backlog using: (a) historical avg cost per [goal][risk] from app.stats, (b) current backlog task count broken down by those dimensions, (c) velocity factor from framework.usage.velocityFactor. Write the forecast to framework.budget.forecastUsd and framework.budget.forecastCycles. Expose it via GET /api/status (embed in StatusResult) so the dashboard can show "estimated X cycles / $Y to clear backlog". Keeps the operator informed before spend commits. goal: intelligence risk: 2
