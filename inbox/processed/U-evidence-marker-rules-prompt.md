reliability: inject Evidence Gate marker rules into the Implementer prompt to prevent L-0005-class failures — lesson L-0005 shows a task was blocked because a contains marker used a template literal instead of a static string. The Implementer never knew the constraint. Fix: in supervisor.mjs where the TASK_CLASS and REVISION_BLOCK are assembled for the implementer, add a section "## EVIDENCE GATE RULES" when task.verifyArtifacts contains entries with `contains`: "Any string declared in verifyArtifacts[].contains MUST appear as a verbatim static string literal in the file — never inside a template literal, string interpolation, or concatenation. Example: console.log('marker: done') not console.log(`marker: ${n} done`)." Also add a pre-flight static scan in evidence.mjs that detects template-literal markers and warns before blocking. goal: reliability risk: 2
