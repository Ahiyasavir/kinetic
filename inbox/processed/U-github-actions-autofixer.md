product: create a GitHub Actions integration — add .github/workflows/kinetic-autofixer.yml that triggers on workflow_run (when CI fails on any branch). The action calls a new endpoint POST /api/ci-fix (added to ui/server.mjs) that: (1) reads the failed workflow's log via GITHUB_TOKEN, (2) creates a kinetic inbox task titled "ci: fix failing workflow on <branch> — <error summary>" with the log excerpt as notes, risk: 3, goal: reliability. The task enters the normal kinetic cycle. A second action step polls GET /api/task-status?id=<id> until merged or blocked, then posts the fix PR link back to the failing PR as a comment. This closes the loop between GitHub CI failures and autonomous fixes — no human needs to triage TypeScript errors or lint failures. goal: product risk: 4
