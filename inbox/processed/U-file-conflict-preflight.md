reliability: add a file-conflict pre-flight guard in the Selector phase — before committing to a task, run a lightweight static scan: parse the top-3 backlog candidates' notes + title for file path references (regex: src/|lib/|apps/|functions/), compare against the set of files touched in the current in_progress task's git worktree (git diff --name-only HEAD). If the candidate overlaps with more than 2 files currently being edited, deprioritize it and pick the next non-conflicting candidate. Log the skip reason to the cycle log. This prevents two consecutive tasks from colliding on the same files, reducing merge conflicts and validation failures after the engine decouples into parallel execution. goal: reliability risk: 3
