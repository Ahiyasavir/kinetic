architecture: implement auto task decomposition — when the Selector picks a task with risk >= 4 AND effort >= 4, before calling the Implementer, invoke a short Haiku "Decomposer" agent that receives the task notes and outputs 2-3 sub-tasks in the standard inbox format. These sub-tasks are injected into the backlog with deps pointing back to each other in sequence and parent_task_id set to the original task id. The original task is moved to blocked with blockReason "decomposed into subtasks: [ids]". The cycle then immediately selects and runs the first sub-task. This mirrors the existing Architect pattern (epics) but is lighter-weight and triggered automatically. Prevents high-risk tasks from causing multi-revision cycles that burn budget. goal: architecture risk: 3
