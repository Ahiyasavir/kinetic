{
  "_comment": "Workspace profile for the WhatsApp Local AI Assistant. Baileys linked-device bridge + local MCP server (stdio) for Claude Desktop/Code. All data stays local; Claude Pro subscription covers the AI cost with zero API billing. Token efficiency is the primary design constraint.",
  "id": "wa-assistant",
  "label": "WhatsApp Local AI Assistant",
  "goalPhases": ["structure", "bridge", "storage", "mcp", "filtering", "integration", "continuous"],
  "metadata": {
    "domain": "local-ai-automation",
    "repo": "wa-assistant",
    "branch": "main",
    "description": "Baileys WhatsApp bridge + MCP server that lets Claude Desktop/Code read, search, summarize, and (with approval) reply to WhatsApp messages — fully local, no API cost, token-efficient."
  },
  "selectionFilters": {
    "shippedFeature": "baileys.*client|auth.?info|qr.*scan|messages\\.upsert|connection\\.update|sqlite.*schema|better.?sqlite|fts5|mcp.*server|stdio.*transport|list.?chats|get.?messages|search.?messages|send.?message|summarize|filter.*pipeline|vip.?list|rate.?limit|claude.*desktop.*config|task.*scheduler",
    "polishIntent": "polish|improv|refin|harden|robust|edge.?case|retry|error handling|timeout|reconnect|backoff|recover|resilient|fallback|guard|sanitiz|validat|disconnect|stale",
    "uiPolish": "qr.*display|terminal.*color|chalk|format.*output|table.*format|spinner|progress.*bar",
    "hardeningKeep": "reconnect|retry|backoff|timeout|error handling|crash|recover|idempoten|race.?condition|duplicate|dedup|disconnect|stale|corrupt|lock|integrity|session.*restore|re.?auth",
    "applyHardeningFreeze": false
  },
  "promptProfile": "wa-assistant conventions (this workspace):\n- WhatsApp connection uses Baileys (@whiskeysockets/baileys) as a linked device — NO Playwright, NO Chrome, NO browser scraping.\n- Session files live in auth_info/ (gitignored). On first run, print QR to terminal via qrcode-terminal. Persist session via useSingleFileAuthState or useMultiFileAuthState from Baileys.\n- All DB access through src/store/db.ts singleton (better-sqlite3, synchronous). Never open a second DB connection.\n- MCP server uses stdio transport (@modelcontextprotocol/sdk). Entry point: `node dist/index.js --mcp` for MCP mode, `node dist/index.js` for daemon mode.\n- send_message tool MUST include a confirmation field in its response: the tool prepares the message but marks it 'pending' — Claude must call a confirm_send tool to actually dispatch it.\n- Hard token budgets per Claude call: summarize ≤ 800 tokens out; QA ≤ 600 tokens out; reply suggestions ≤ 200 tokens out per option (3 options max).\n- Every Claude interaction must log { operation, tokensIn, tokensOut } to ai_calls table.\n- Run the local filter pipeline (src/filter/pipeline.ts) BEFORE any context is built for Claude — if it returns 'skip', no AI call is made.\n- Strict TypeScript (strict:true), no `any`, explicit return types on all exports.\n- Honor CLAUDE.md in the project root.",
  "_foundation_note": "MVP COMPLETE + VERIFIED in the repo. ALL original seed tasks (WA-STORE, WA-BRIDGE, WA-SYNC, WA-MCP-CORE, WA-MCP-SEARCH, WA-MCP-SEND, WA-MCP-SUMM, WA-FILTER, WA-CONFIG, WA-MCP-READ) are implemented, tested, and committed. The product boots end-to-end: the daemon connects to WhatsApp (latest-version negotiation) and emits a scannable pairing QR; once paired, messages sync into SQLite and 9 MCP tools (list_chats, get_messages, get_unread, search_messages, send_message, confirm_send, summarize_chat, persist_summary) serve Claude Desktop/Code over stdio. Verified: tsc clean, full build clean, 10/10 filter unit tests + multiple store/gate/summarize/read runtime smoke tests pass, daemon QR boot confirmed. The seed is now EMPTY — kinetic should propose the NEXT most valuable improvement (continuous phase): suggestions = reply-suggestion tool (3 drafted options via the existing send gate), QA-over-history tool (FTS-first then summary fallback), voice-note transcription pipeline, allowlist/security hardening, or a control-room web UI. Read the existing src/ before building; do NOT re-create the store/bridge/server scaffolding.",
  "seed": []
}
